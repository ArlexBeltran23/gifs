name: Say hello

on:
  push:
    branches:
      - master

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: echo the message
        run: |
          echo "Hola midu!"
      - name: dime la fecha
        run: date
      - run: ls -l
      